import { navigationData } from './data/navigation';

export const headerData = {
  links: navigationData.header.links,
  actions: navigationData.header.actions,
};

export const footerData = navigationData.footer;
