---
export interface Props {
  visualSubheading?: string;
  subtitle: string;
}

const { visualSubheading, subtitle } = Astro.props;
const paragraphs = subtitle
  .split(/\n\n+/)
  .map((paragraph) => paragraph.trim())
  .filter(Boolean);
---

{
  visualSubheading && (
    <span class="block text-[20px] md:text-[24px] font-normal font-heading text-heading mb-6 leading-[130%] tracking-tight">
      {visualSubheading}
    </span>
  )
}
{
  paragraphs.map((paragraph, index) => (
    <>
      {index > 0 && (
        <>
          <br />
          <br />
        </>
      )}
      {paragraph}
    </>
  ))
}
