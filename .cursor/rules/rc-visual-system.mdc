---
description: R&C Roofing — site-wide visual system. Section color flow, hero, grey, black, and CTA rules for every page on this site.
alwaysApply: true
---

# R&C Roofing — Visual Design System

> **Canonical reference:** `src/pages/index.astro` is the finished reference page. When in doubt, match it exactly.
> This rule is **project-specific** and must not be applied to any other project.

---

## Required Import

Every page should import `SectionBg` for consistent section backgrounds:

```astro
import SectionBg from '~/components/ui/SectionBg.astro';
```

The `SectionBg` component handles all background patterns internally — no need to import `simplePattern` directly.

Site-wide motif (shape, which sections, fade, tile size) lives in `src/config/motif.ts`. Colors and opacity live in `CustomStyles.astro`. Do not hardcode pattern SVGs on pages.

---

## Section Color Flow

Sections repeat in this order down every page. Never skip or reverse the order.

```
White → Grey (#FAFAFA) → Black → White → Grey → Black → … → Yellow (Final CTA)
```

---

## 1 · Hero Section (always White bg)

- Component: `Hero2`
- **Default (`variant="split"`):** left content / right **full-height** image, flush to the section edges. White with yellow accent motif at **10% opacity**, top-to-bottom fade. Image left: `isReversed`.
- **Homepage only (`variant="overlay"`):** the `fancy-two.svg` artwork covers the full hero, text left, no side image. Readability comes from a left-to-right black scrim — never lay a yellow motif tile over it
- Left side: H1, subtitle paragraphs, primary CTA button
- No eyebrow text. No gold bar. No `isDark`.
- **Do NOT add `tag-pill` badges unless they are explicitly listed in the content brief.** Omit `<Fragment slot="content">` entirely when badges are not provided.

```astro
<Hero2
  actions={[{ variant: 'primary', text: 'CTA Text', href: '/contact' }]}
  image={{ src: 'IMAGE_URL', alt: 'Alt text' }}
>
  <Fragment slot="title">Page H1<br />Line Two</Fragment>
  <Fragment slot="subtitle">
    First paragraph.
    <br /><br />
    Second paragraph.
  </Fragment>
  <!-- Only include the content slot when badges are explicitly given in the content brief -->
  <!-- <Fragment slot="content">
    <div class="flex flex-wrap gap-3 mt-6">
      <span class="tag-pill">Badge One</span>
    </div>
  </Fragment> -->
  <Fragment slot="bg">
    <SectionBg variant="hero" />
  </Fragment>
</Hero2>
```

---

## 2 · Grey Section Rules (`bg-[#FAFAFA]`)

- Background: always exactly `#FAFAFA`
- Headings: `text-heading` (`#222`)
- Body text: `text-muted` (`#444` — subtle black; never washed-out grey, never pure `#000`)
- No motif
- **Cards are BLACK** — dark cards on a light grey background create intentional contrast

### Grey section bg snippet
```astro
<Fragment slot="bg">
  <SectionBg variant="grey" />
</Fragment>
```

### Card on Grey bg
```astro
<div class="rounded-lg border border-accent/60 bg-[#222] p-4 md:p-5 flex flex-col shadow-none outline-none">
  <div class="mb-4 w-10 h-10 rounded-md border border-accent/60 flex items-center justify-center">
    <Icon name="tabler:icon-name" class="w-5 h-5 text-accent" />
  </div>
  <h3 class="text-[16px] md:text-[18px] font-medium leading-[130%] mb-2 text-white">{title}</h3>
  <p class="text-[14px] md:text-[15px] font-normal leading-[160%] text-white/60">{description}</p>
</div>
```

Prefer `<InfoCard title description icon variant="dark" />` over handmade markup.

Optional left accent bar for compact cards: add `border-l-4 border-l-accent` to the card div.

### Ghost button on Grey bg

```astro
<a href="/path" class="btn-ghost-light">Button Label</a>
```

Or use the Button component:
```astro
<Button variant="ghost-light" text="Button Label" href="/path" />
```

### Step/numbered circle on Grey bg

When a step or process uses a numbered circle indicator, the circle must be dark with yellow text:

```astro
<div class="shrink-0 w-9 h-9 rounded-full bg-[#222] flex items-center justify-center font-heading font-normal text-[15px] text-accent mt-0.5">
  {step}
</div>
```

- Circle bg: `bg-[#222]`
- Number text: `text-accent` (yellow)
- Never use `bg-primary`, `bg-white`, or any other colour for step circles on a grey background

---

## 3 · Black Section Rules (`bg-black` + motif)

- Background: `bg-black`
- Motif: `simple.svg` accent yellow at **12% opacity**, top-to-bottom fade
- Headings: `text-white`
- Body text: `text-white/70`
- Disclaimer / secondary text: `text-white/50`
- **Cards are dull black with yellow border**

### Black section bg snippet
```astro
<Fragment slot="bg">
  <SectionBg variant="dark" />
</Fragment>
```

### Card on Black bg
```astro
<div class="rounded-lg border border-accent/60 bg-[#222] p-6 flex flex-col">
  <h3 class="text-[22px] md:text-[26px] font-medium leading-[130%] mb-3 text-white">{title}</h3>
  <p class="text-[14px] md:text-[15px] font-normal leading-[160%] text-white/60">{description}</p>
</div>
```

Use `isDark={true}` on `WidgetWrapper` for black sections. Pass `cardClass="border border-accent/60 bg-[#222]"` to `Features2` or `Testimonials` components.

### Ghost button on Black bg

```astro
<a href="/path" class="btn-ghost-dark">Button Label</a>
```

Or use the Button component:
```astro
<Button variant="ghost-dark" text="Button Label" href="/path" />
```

---

## 4 · White Section Rules (`bg-white`)

- Background: white (no bg div needed, or explicit `bg-white`)
- Headings: `text-heading` (`#222`)
- Body text: `text-muted` (`#444` — subtle black; never washed-out grey, never pure `#000`)
- No motif

### White section bg snippet
```astro
<Fragment slot="bg">
  <SectionBg variant="white" />
</Fragment>
```

### Card on White bg
```astro
<div class="rounded-lg bg-card-light p-4 md:p-5 flex flex-col">
  <div class="mb-4 w-10 h-10 rounded-md bg-[#222] flex items-center justify-center">
    <Icon name="tabler:icon-name" class="w-5 h-5 text-accent" />
  </div>
  <h3 class="text-[18px] md:text-[20px] font-medium leading-[130%] mb-1 text-heading">{title}</h3>
  <p class="text-[14px] md:text-[15px] font-normal leading-[160%] text-muted">{description}</p>
</div>
```

Prefer `<InfoCard title description icon variant="light" border={false} />` over handmade markup.

- Background: yellow tint (`rgb(237 217 116 / 50%)`) — same as CTA banner style
- No border on light cards
- **Links on yellow cards:** `text-heading` (`#222`) / `text-card-link-light`. Never `text-primary` or `text-accent` — gold and slate both blend into the wash. Same rule if a yellow card sits on a grey section.

### Ghost button on White bg

```astro
<a href="/path" class="btn-ghost-light">Button Label</a>
```

Or use the Button component:
```astro
<Button variant="ghost-light" text="Button Label" href="/path" />
```

---

## 5 · Final CTA Banner (always last section on every page)

- The `WidgetWrapper` section always sits on a **white background** (no bg override needed on the wrapper itself)
- The inner card: `bg-cta` (`#F5ECBD` / `--aw-color-bg-cta`) — **no border on the card**
- Motif: from `src/config/motif.ts` + `--aw-color-motif-cta` / `--aw-opacity-motif-cta` (R&C: black chevron at 2%). `CTABanner` applies it — do not paste mask CSS.
- Heading: `text-black`
- Body text: `text-black/70`
- Contact info: `text-black/70`, name bold `text-black`
- Separator: `border-t-2 border-black`
- CTA button: `bg-black text-white hover:bg-zinc-900`

```astro
<CTABanner title="CTA Heading" subtitle="Supporting sentence." />
```

---

## 6 · Testimonials Section

Testimonials follow the section color flow but have special rules when using black cards on grey backgrounds:

### Testimonials on Grey bg with black cards
When placing Testimonials on a grey section with black cards (`cardClass="border border-accent/60 bg-[#222]"`):
- Set `isDark={true}` — this makes card text (name, job, testimonial) white
- Override the headline with explicit dark text classes to keep the section title dark

```astro
<Testimonials
  title="Section Title"
  subtitle="Subtitle text"
  isDark={true}
  cardClass="border border-accent/60 bg-[#222]"
  classes={{ headline: { title: 'text-[26px] md:text-[40px] font-normal leading-[120%] text-[#111111]', subtitle: 'text-[16px] md:text-[17px] font-normal leading-[160%] text-[#444444]' } }}
  testimonials={[...]}
  callToAction={{
    variant: 'ghost-light',
    text: 'Button Text',
    href: '/path',
  }}
>
  <Fragment slot="bg">
    <SectionBg variant="grey" />
  </Fragment>
</Testimonials>
```

**Key rules:**
- `isDark={true}` for white text on black cards
- Explicit headline classes for dark section title on grey bg
- CTA button uses `variant: 'ghost-light'` (black text/border on grey)

---

## 7 · FAQ Section (follows the section color flow)

FAQ sections are **not fixed to a specific background** — they take whatever background colour falls at their position in the page's colour flow (White → Grey → Black).

- **No cards** — FAQ is an accordion with no card wrappers around items
- The `FAQs` component (or equivalent accordion) is used directly
- Text contrast must match the background:

| Background | Section heading | Question text | Answer text |
|---|---|---|---|
| White | `text-heading` | `text-heading` | `text-muted` (`#444`) |
| Grey (`#FAFAFA`) | `text-heading` | `text-heading` | `text-muted` (`#444`) |
| Black | `text-white` | `text-white` | `text-accent` (yellow) |

### FAQ on White or Grey bg
```astro
<Fragment slot="bg">
  <!-- White: nothing needed -->
  <!-- Grey: -->
  <div class="absolute inset-0 bg-[#FAFAFA] dark:bg-slate-900/30"></div>
</Fragment>
```

### FAQ on Black bg — pass `isDark` and bg snippet

When `isDark={true}` is set, the `FAQs` component automatically applies:
- Section heading: `text-white`
- Question text: `text-white`
- Answer text: `text-accent` (yellow)
- Dividers/borders: `white/20`
- Toggle icon: `border-white/30 text-white/50`, open state `border-accent text-accent`

```astro
<FAQs
  isDark={true}
  title="Frequently Asked Questions"
  items={[...]}
>
  <Fragment slot="bg">
    <SectionBg variant="dark" />
  </Fragment>
</FAQs>
```

---

## Hard Rules (never break these)

| Rule | Detail |
|---|-----|
| No `card-hover` | Cards are always static — no lift/shadow on hover |
| No gold bar | Never add `<span class="gold-bar">` under any heading |
| No eyebrow text | No tagline/eyebrow above H2 headings |
| Grey is always `#FAFAFA` | Never use `bg-gray-50` or any other grey value |
| Black card bg is always `#222` | Use `bg-[#222]` — not `bg-zinc-800`, not `bg-black` |
| Use SectionBg component | Always use `<SectionBg variant="..." />` for section backgrounds — never manual pattern code |
| Motif is site config | Shape / fade / tile / which sections: `src/config/motif.ts`. Color / opacity: `CustomStyles.astro`. R&C: hero 10%, dark 12%, CTA 2% |
| Hero badges only when explicit | Never add `tag-pill` badges unless the content brief lists them |
| CTA card has no border | The yellow card uses `bg-cta` (`#F5ECBD`) only — no `border` class |
| FAQ has no cards | FAQ is a plain accordion — never wrap items in card divs |
| FAQ on Black bg | `isDark={true}` — heading `text-white`, questions `text-white`, answers `text-accent` (yellow) |
| Ghost button on Black | Use `btn-ghost-dark` class or `variant="ghost-dark"` |
| Ghost button on White/Grey | Use `btn-ghost-light` class or `variant="ghost-light"` |
| Testimonials on Grey with black cards | Set `isDark={true}` for white text on cards, override headline with explicit dark text classes |
| Card on White section | Yellow tint background (`50%`), no border |
| Card on Grey section | Black background (`bg-[#222]`), yellow border, white text |
| Simple heading+description cards | Tabler icon mark above the title — light cards `bg-[#222]` square, dark cards gold-border square, icon `text-accent` |
| Light-section body text | On white / grey / yellow-tint: `text-muted` = `#444`. Never `#A2AFB5`, `text-gray-400`, or `text-black` |
| Links on yellow cards | `text-heading` / `text-card-link-light` (`#222`). Never `text-primary` or `text-accent` — they blend into the yellow |
| Card link / button | Always bottom-aligned: card `flex flex-col h-full`, description `flex-1`, CTA `mt-auto` |
